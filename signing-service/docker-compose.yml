version: '3.8'

services:
  signing-service:
    build: .
    container_name: ton-signing-service
    restart: unless-stopped
    ports:
      - "5000:5000"
    env_file:
      - .env
    volumes:
      - ./logs:/app/logs
